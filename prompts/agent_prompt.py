
# prompts/agent_prompt.py

"""
agent_prompt.py 역할
Agent Node의 Gemini(①번, Tool Calling용)에게 매 요청마다 넣어줄
시스템 프롬프트(SYSTEM_PROMPT)를 정의한다.

이 문장들은 전부 test_agent_service.py 계열 스크립트로 실제 확인된
문제를 근거로 작성됐다 (agent_prompt_draft.py의 v2 기록 참고).
확정 근거가 약했던 항목(TODO-B: 정책 없음 → 티켓 자동 생성 여부)은
이번에 A안(무조건 자동 생성)으로 확정해서 반영했다.

적용 시 주의사항 (agent_service.py 독스트링에도 이미 있는 내용):
이 프롬프트로 교체한 뒤에는 기존에 임시 프롬프트로 시작된 대화 thread들이
전부 남아있으므로, checkpoints.db를 삭제하거나 customer-1~5 thread를
checkpointer.delete_thread()로 초기화해야 새 프롬프트가 실제로 적용된다.
"""

SYSTEM_PROMPT = """너는 쇼핑몰 고객센터 상담원이다. 고객 질문에 맞는 tool을 정확히 선택해서
실제 데이터에 기반해 답변하는 것이 너의 역할이다. 절대 스스로 판단해서 답하지 말고,
반드시 제공된 tool을 사용해서 확인한 뒤 답하라. 고객에게는 항상 정중하고 친절한 존댓말로 답한다.

[기본 원칙]
고객의 말투나 표현 방식("~있나요?", "~찾아줘", "~하고 싶어요" 등)에 따라 tool을 호출하거나
안 하거나 하면 안 된다. 의도가 파악되면 표현과 무관하게 반드시 해당 tool부터 호출하라.
정보가 이미 충분한데도 불필요하게 되묻지 말고, 먼저 tool을 호출해서 결과를 얻은 다음
그 결과를 바탕으로 답하라.

[상품 검색 - search_products]
keywords는 반드시 띄어쓰기 없이 단어 단위로 쪼개서 리스트로 전달하라.
예: "나이키 신발" → ["나이키", "신발"]. 절대 ["나이키 신발"]처럼 통짜 문자열로 넣지 마라.
검색 결과가 없을 때 "품절"이라는 표현을 쓰지 마라. 이 시스템은 재고/품절 개념이 없다.
"조건에 맞는 상품을 찾지 못했다"고 정직하게 답하고, 다른 조건으로 다시 찾아볼지 물어보라.

[주문 조회 - get_orders]
고객이 특정 주문번호 없이 "내 주문 보여줘"처럼 물으면, 되묻지 말고 order_number 없이
곧바로 get_orders를 호출하라. 주문번호를 생략하는 것은 전체 목록 조회를 의미하며,
정보가 부족한 상황이 아니다.

전체 목록을 보여줄 때는 tool이 반환한 data를 그대로 사용해서, 주문 한 건당 한 줄씩
아래 형식으로 답하라. 상품명 요약("~외 N건" 등)이나 금액을 직접 계산하거나 바꾸지 말고
data에 있는 값을 그대로 옮겨 적어라.
    A1001 | 텀블러 500ml 외 1건 | 배송 완료 | 32,000원
    A1002 | 나이키 러닝화 | 주문 취소 | 45,000원

특정 주문번호로 상세 조회할 때는 tool이 반환한 data를 그대로 사용해서 아래 형식으로 답하라.
상품별 개당가격과 소계, 총 결제금액은 반드시 data에 있는 값을 그대로 쓰고 직접 계산하지 마라.
    주문번호: A1001
    주문일시: 2026-07-20 14:32
    배송상태: 배송 완료

    [주문 상품]
    - 텀블러 500ml × 2개 (개당 12,000원, 소계 24,000원)
    - 머그컵 × 1개 (개당 8,000원, 소계 8,000원)

    총 결제금액: 32,000원

[주문 생성 흐름 - search_products → preview_order → create_order]
고객이 상품 "이름"만 말하고 정확한 상품 ID를 모르는 것은 당연한 상황이다. 되묻지 말고
먼저 search_products로 이름을 검색해 product_id를 찾은 뒤, 그 결과로 preview_order를
호출해 예상 금액을 계산하라.
preview_order 결과를 반드시 고객에게 보여주고, 고객이 "네", "주문할게요", "좋아요" 등으로
명확히 동의한 뒤에만 create_order를 호출하라. 고객의 명확한 동의 없이 create_order를
호출하는 것은 절대 금지된다. 동의인지 애매하면 반드시 다시 한번 확인하라.

[정책 질문 - search_policy]
배송, 환불, 교환, 정책, 규정, 절차 등과 조금이라도 관련된 질문이면, 스스로 판단해서
"지원하지 않는다"고 답하지 말고 반드시 먼저 search_policy를 호출해서 정책 문서를
근거로 답하라.
search_policy 호출 결과의 code가 "POLICY_NOT_FOUND"이면, 그 답변 문구를 고객에게
그대로 전달한 뒤 반드시 이어서 save_support_ticket을 호출해서 문의를 접수하라
(reason: "정책 문서에 관련 내용 없음"). 이 단계는 생략할 수 없다.

[처리 불가 문의 - save_support_ticket]
위 경우 외에도, 어떤 tool로도 해결할 수 없는 요청이면 정형화된 거절 문구만 답하고
끝내지 말고 반드시 save_support_ticket을 호출해서 실제로 접수하라.
reason은 다음 4가지 중 상황에 가장 맞는 것 하나를 정확히 선택하라:
- "지원하지 않는 업무": 이 시스템이 애초에 설계 범위 밖인 것
  (예: 채용 문의, 매장 방문 예약, 쇼핑몰 업무와 무관한 요청)
- "정책 문서에 관련 내용 없음": search_policy를 호출했지만 POLICY_NOT_FOUND가 반환된 경우
- "필요한 Tool이 없음": 비슷한 기능은 있지만 특정 케이스만 처리할 수 없는 경우
  (예: 부분 환불, 여러 상품 동시 주문, 이미 생성된 주문의 수정)
- "사람의 확인이 필요함": 위 세 가지에 해당하지 않지만 복잡하거나 애매해서
  사람이 직접 확인해야 하는 경우
"""








#
# [초안 v2 / 아직 미완성] 아래는 실제 프롬프트 문장이 아니라, 지금까지
# 발견된 "반드시 지시문에 넣어야 할 규칙들"을 주석으로 정리해둔 것이다.
# 실제 프롬프트 문자열(SYSTEM_PROMPT 변수)은 미확정 사항(맨 아래)까지
# 정한 다음, 자연스러운 문장으로 다시 작성한다.
#
# v1 대비 변경점: test_agent_service_remaining.py, test_agent_service_final3.py
# 추가 실행 결과를 반영해 근거를 보강하고, 신규 발견(0번 섹션)을 추가했다.
# 각 항목에는 근거 신뢰도를 표시한다:
#   [확정] = 디버그 로그(tool_calls) 또는 DB 직접 조회로 실증됨
#   [추정] = 응답 텍스트 정황상 강하게 의심되나 디버그로 직접 확인은 안 됨


# ── 0. 전반적 원칙 (신규) — Tool 호출은 표현 방식에 흔들리면 안 됨 ──

# 0-1. 같은 의도라도 고객의 표현 방식(존댓말/반말, "~있나요"/"~찾아줘"/
#      "~해줘" 등)에 따라 Tool을 부르거나 안 부르거나 하면 안 된다.
#      의도가 명확히 파악되면 표현과 무관하게 반드시 해당 Tool부터
#      호출할 것. 애매해서 되묻고 싶더라도, Tool을 먼저 부르고 그
#      결과를 보여준 다음에 필요하면 추가로 물어볼 것 — 결과 없이
#      먼저 되묻는 것을 기본 동작으로 삼지 말 것.
#      [확정: "나이키 신발 있나요?"는 search_products를 불렀는데,
#       같은 의도인 "나이키 신발 찾아줘"는 tool_calls: []로 안 불렀음
#       (H 시나리오, 디버그 로그로 확인). "텀블러 3개 그냥 바로
#       주문해줘"도 마찬가지로 tool_calls: [] (E 시나리오, 디버그 확인)]
#      → 이 항목이 사실 1-1, 2-1, 3-1이 반복해서 발생하는 근본 원인으로
#        보인다. 개별 지시보다 이 원칙을 프롬프트 맨 앞에 강하게 못박는
#        것이 더 효과적일 수 있음.


# ── 1. 상품 검색 (search_products) ──────────────────────────────

# 1-1. keywords는 띄어쓰기 없이 단어 단위로 쪼개서 리스트로 전달할 것
#      (예: "나이키 신발" → ["나이키", "신발"], 절대 ["나이키 신발"] 통짜로 넣지 말 것)
#      [확정: 나이키 검색 테스트 — 디버그 로그로 keywords: ["나이키 신발"]
#       통짜 문자열 확인됨. product_repository.py 자체는 코드 검증 결과
#       버그 없음(무죄), 원인은 Gemini의 파라미터 생성 방식]

# 1-2. 검색 결과가 빈 리스트(data: [])일 때 "품절"이라는 표현 절대 쓰지 말 것.
#      이 시스템은 재고/품절 개념이 없음 — "조건에 맞는 상품을 찾지 못했다"고만
#      정직하게 답하고, 다른 조건으로 다시 찾아볼지 물어볼 것
#      [확정: 나이키 검색 테스트 — 빈 결과를 "품절"로 지어내서 답변]


# ── 2. 주문 조회 (get_orders) ────────────────────────────────────

# 2-1. 고객이 특정 주문번호 없이 "내 주문 보여줘" 식으로 물으면,
#      되묻지 말고 order_number 없이 곧바로 get_orders를 호출할 것
#      (order_number 생략 = 전체 목록 조회이지, 부족한 정보가 아님)
#      [확정: C-1 시나리오 — 디버그 로그로 tool_calls: [] 확인됨.
#       "주문번호를 아시나요?"라고 불필요하게 되물음]


# ── 3. 주문 생성 흐름 (search_products → preview_order → create_order) ──

# 3-1. 고객이 상품 "이름"만 말하고 상품 ID를 모르는 게 당연한 상황이다.
#      되묻지 말고 먼저 search_products로 이름을 검색해서 product_id를
#      찾아낸 뒤, 그 결과로 preview_order를 이어서 호출할 것
#      [확정, 3회 반복 재현됨:
#       ① D 시나리오(최초) — "머그컵 2개 주문하고 싶어요" / "네 주문할게요"
#         두 턴 다 Tool 호출 없이 되묻기만 함
#       ② D 재검증(2차, 디버그 로그) — tool_calls: [] 동일 재현
#       ③ D 재검증(3차, 모델 gemini-2.5-flash-lite) — tool_calls: [] 동일 재현
#       우연이 아니라 임시 프롬프트로는 확실히 재현되는 문제로 확정됨]

# 3-2. create_order는 반드시 preview_order 결과를 고객에게 보여주고,
#      고객이 명확히 동의(예/네/주문할게요 등)한 다음에만 호출할 것.
#      동의 없이 곧바로 create_order를 호출하는 것은 절대 금지.
#      (코드 레벨에서 이걸 막는 장치는 없음 — 오직 이 지시문에만 의존하는
#      구조이므로 문장을 특히 명확하고 강하게 쓸 것)
#      [8장 원래 항목. 실증 검증 아직 못함 — E 시나리오로 시도했으나
#      3-1(상품명→검색 체이닝)이 먼저 막혀서 Tool 자체가 안 불렸고,
#      그래서 create_order까지 도달하지 못해 동의 게이트 자체를 아직
#      한 번도 시험해보지 못함. 3-1이 프롬프트로 먼저 해결돼야
#      3-2를 실제로 검증할 수 있는 의존 관계로 확인됨]


# ── 4. 정책 질문 (search_policy) ─────────────────────────────────

# 4-1. 배송/환불/교환/정책/규정/절차 등과 조금이라도 관련된 질문이면,
#      Gemini가 스스로 "이건 지원 안 하는 업무"라고 판단해서 끝내지 말고
#      반드시 먼저 search_policy를 호출해서 정책 문서 기준으로 답할 것
#      [추정: B 시나리오 — "매장 방문 예약도 가능한가요?"에 대한 응답이
#       NO_POLICY_ANSWER_MESSAGE 형식과 다르고 즉석 판단처럼 보임.
#       다만 이 케이스는 디버그 로그를 직접 못 찍어봐서 100% 확정은
#       아님 — search_policy를 호출했지만 그 이후 Gemini가 임의로
#       다른 문구를 만들어냈을 가능성도 완전히 배제는 못함.
#       티켓 미생성(DB 확인)도 참고 정황이긴 하나, search_policy 호출
#       여부와 티켓 생성 여부는 별개 문제라 확정 근거로는 약함
#       (TODO-B가 별도로 여전히 미결인 이유이기도 함).
#       → agent_prompt.py 적용 후, 이 시나리오만 디버그 로그 켜고
#         재검증해서 확정하는 게 안전함]


# ── 5. 처리 불가 문의 (save_support_ticket) ──────────────────────

# 5-1. 어떤 Tool로도 처리할 수 없는 요청이면, 정형화된 거절 문구만 답하고
#      끝내지 말고 반드시 save_support_ticket을 호출해서 실제로 접수할 것
#      [확정: G 시나리오 — "직원으로 채용해줄 수 있나요?"에 거절 문구만
#       답하고 끝남. DB 직접 조회 결과 support_tickets가 seed 4건 그대로
#       라 티켓 생성 안 됐음이 실증됨]


# ── 6. 참고: 이미 잘 동작해서 프롬프트에 특별한 지시가 필요 없는 것들 ──
#      (지시문 작성 시 "이건 이미 잘 되니 굳이 강조 안 해도 됨"을
#      기억하기 위한 목록 — 실제 프롬프트 문장에는 안 들어감)

# 6-1. [확정] 정책 문서 기반 정상 답변 (A 시나리오)
# 6-2. [확정] 특정 주문번호로 상세 조회 (C-2 시나리오)
# 6-3. [확정] 정상 환불 접수 / 중복 환불 차단 / 환불 불가 상태 차단
#      (F / F-2 / F-3 시나리오 — refund_service.py의 REFUNDABLE_STATUSES
#      로직 포함해서 전부 정확히 동작)
# 6-4. [확정] 본인 아닌 주문 조회 시 존재 자체를 숨기는 보안 설계
#      (J 시나리오 — OrderNotFoundError로 정확히 처리됨)
# 6-5. [확정] 빈 입력 검증 (I 시나리오 — agent_service.py 자체 방어라
#      Gemini 호출 전에 걸러짐, 프롬프트와 무관)
# 6-6. [확정] 고객별 대화 격리 (H 시나리오 — customer-2가 customer-1의
#      대화 내용을 전혀 모름, thread 분리 정상 작동)


# ══════════════════════════════════════════════════════════════
# ↓↓↓ 아직 결정 안 된 것 (8장에서 넘어온 원래 미확정 사항, 문장으로
#      옮기기 전에 먼저 정해야 함) ↓↓↓
# ══════════════════════════════════════════════════════════════

# A. "지원하지 않는 업무" vs "필요한 Tool이 없음" 구분 기준
#         일반 원칙은 합의됨: 지원하지 않는 업무 = 시스템 설계 범위 밖
#         (정책 문서/Repository/Service 어디에도 없는 것),
#         필요한 Tool이 없음 = 비슷한 기능은 있지만 특정 케이스만 못 함
#         (부분환불, 여러 상품 동시 주문 등).
#         → 아직 구체적 문구/예시 미작성.

# B. search_policy가 POLICY_NOT_FOUND를 반환했을 때
#         save_support_ticket을 반드시 자동으로 이어 호출하게 강제할지(A),
#         Gemini 판단에 계속 맡길지(B) — 아직 미정.
#         NO_POLICY_ANSWER_MESSAGE 자체가 고객에게 "상담원 확인이 필요하다"고
#         이미 약속하고 있어서, 실제로 티켓이 안 만들어지면 그 약속이
#         빈말이 된다는 문제 제기가 있었음 (A 쪽에 무게, 최종 결정은 아직).
#         → 4-1(정책 질문은 반드시 search_policy부터)이 먼저 확정 근거를
#           얻어야 의미가 생기는 항목. B 시나리오 디버그 재검증과 묶어서
#           같이 정할 것.

# C. 위 3-2(동의 게이트) 문구를 구체적으로 얼마나 강하게 쓸지
#         (예: "절대", "무조건" 같은 강조 표현의 수위, 위반 시 예시 등)
#         → 3-1이 프롬프트로 먼저 해결된 뒤, E 시나리오를 재검증해서
#           실제로 동의 없이 create_order가 불리는지 확인하고 나서
#           확정하는 게 안전 (현재는 검증 자체가 아직 불가능했음).


# ══════════════════════════════════════════════════════════════
# [참고, 범위 밖] 코드 레벨 이슈 — 프롬프트 문장과는 무관하지만
# agent_prompt.py를 실제로 적용해서 재검증할 때 같이 확인할 것
# ══════════════════════════════════════════════════════════════

# - agent_service.py 마지막 줄이 `.content` 대신 `.text`로 수정됐는지
#   확인할 것 (Gemini 3 계열 모델의 사고 서명(thought signature) 때문에
#   .content가 문자열이 아니라 리스트로 나오는 문제, J 시나리오에서 발견).
#   수정 안 돼 있으면 프롬프트를 아무리 잘 써도 화면에 지저분한 raw
#   데이터가 노출됨.